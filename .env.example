# Server Configuration
PORT=5000
# Ensure you have a Neon account and database provisioned. 
# See https://neon.tech/docs/guides/secure-connections#get-the-connection-string
# The part of the URL that says [YOUR-PROJECT-NAME] is the name of your Neon project.
# The part of the URL that says [YOUR-ROLE-NAME] is the name of your Neon project's role.
# The part of the URL that says [YOUR-PASSWORD] is your role's password.
DATABASE_URL="postgresql://[YOUR-ROLE-NAME]:[YOUR-PASSWORD]@[YOUR-PROJECT-NAME].neon.tech/neondb?sslmode=require"

# JWT Secret for token signing (replace with a strong, unique secret)
JWT_SECRET="your-super-secret-and-long-jwt-key-for-production"

# Super Admin Credentials
# Set these to define the global super administrator account.
# The SUPER_ADMIN_PASSWORD should be a bcrypt hash of the desired password.
# You can generate a bcrypt hash using an online tool or a script like this:
# const bcrypt = require('bcrypt');
# const saltRounds = 10;
# const plainPassword = 'yourChosenSecurePassword';
# bcrypt.hash(plainPassword, saltRounds, function(err, hash) {
#   if (err) {
#     console.error('Error hashing password:', err);
#   } else {
#     console.log('Bcrypt hash:', hash);
#   }
# });
SUPER_ADMIN_USERNAME="superadmin"
SUPER_ADMIN_BCRYPT_HASH="$2b$10$YOUR_BCRYPT_HASH_HERE" # Replace with an actual bcrypt hash

# Clerk Configuration (REMOVED - Not actively used in the current setup)
# If Clerk integration is desired in the future, these variables would be needed:
# CLERK_PUBLISHABLE_KEY=""
# CLERK_SECRET_KEY=""
# CLERK_SIGN_IN_URL="/sign-in"
# CLERK_SIGN_UP_URL="/sign-up"
# CLERK_AFTER_SIGN_IN_URL="/dashboard"
# CLERK_AFTER_SIGN_UP_URL="/dashboard"

# Vite/Frontend specific environment variables (if any, prefix with VITE_)
# Example:
# VITE_API_BASE_URL="http://localhost:5000/api"
